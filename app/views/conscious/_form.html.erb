<%= form_with(model: consciou) do |form| %>
  <% if consciou.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consciou.errors.count, "error") %> prohibited this consciou from being saved:</h2>

      <ul>
        <% consciou.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
