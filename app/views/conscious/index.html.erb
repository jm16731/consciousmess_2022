<p id="notice"><%= notice %></p>

<h1>Conscious</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @conscious.each do |consciou| %>
      <tr>
        <td><%= link_to 'Show', consciou %></td>
        <td><%= link_to 'Edit', edit_consciou_path(consciou) %></td>
        <td><%= link_to 'Destroy', consciou, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Consciou', new_consciou_path %>
